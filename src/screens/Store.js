import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

const Store = () => {
    return (
        <View>
            <Text>Store</Text>
        </View>
    );
};

export default Store;
